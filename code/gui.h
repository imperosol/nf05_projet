#include "struct.h"

#ifndef GUI_H
#define GUI_H

void display_top_square(SDL_Renderer* renderer, SDL_Surface* icon, SDL_Color text_color, const int width, const int height);

void display_icon(SDL_Window* window);

#endif // GUI_H
